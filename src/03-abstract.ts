import {Animal, Dog} from './01-class';
//usar la palabra abstract en la clase padre va a impedir que se creen instancias de el, ejemplo
//no queremos que se creen objetos de tipo Animal

//let myAnimal = new Animal('Nutella');

const chato = new Dog('chato', 'jose');
